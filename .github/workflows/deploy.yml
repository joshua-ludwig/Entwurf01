name: Deploy to Hostinger

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 82.25.96.10
          username: u107363954.entwurf
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./
          server-dir: /home/u107363954/domains/joshua-entwurf.de/public_html/
